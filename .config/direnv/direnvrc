# -*- mode: sh -*-

pass_get() {
	if test "$2"; then
		pass "$1" | sed -n "/^$2: /s///p"
	else
		pass "$1" | sed 1q
	fi
}

use_guix() {
	if test -e ./.guix-profile/etc/profile; then
		. ./.guix-profile/etc/profile
	else
		eval "$(guix environment --root=./.guix-profile "$@" --search-paths)"
	fi
}
